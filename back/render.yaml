services:
  - type: web
    name: louvourhub-back
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node --max-old-space-size=400 dist/main
    envVars:
      - key: NODE_ENV
        value: production
